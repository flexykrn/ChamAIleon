services:
  - type: web
    name: chameleon-defense-api
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: uvicorn classify_api:app --host 0.0.0.0 --port $PORT
    envVars:
      - key: PYTHON_VERSION
        value: 3.10.11
